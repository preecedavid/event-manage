<section class="section">
  <h1>Home#index</h1>
  <p class="lead">
    <%= current_user ? "Welcome, #{current_user.first_name}!" : "Welcome" %>
  </p>
</section>

<% content_for(:page_title, 'Welcome') %>
